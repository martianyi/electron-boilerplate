{
  "name": "electron-seed",
  "version": "0.1.0",
  "description": "Using electron to build an realtimecat app",
  "main": "app.js",
  "scripts": {
    "start": "gulp electron-start",
    "build": "electron-packager . ElectronTest --out=dist --platform=darwin --arch=x64 --version=0.36.7 --asar --overwrite --ignore=node_modules/electron-* --ignore=node_modules/gulp-*",
    "rebuild": "electron-rebuild",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Yi Kai",
  "license": "ISC",
  "devDependencies": {
    "electron-connect": "~0.3.7",
    "electron-packager": "^5.2.1",
    "electron-prebuilt": "^0.36.7",
    "electron-rebuild": "^1.1.3",
    "gulp": "^3.9.0",
    "gulp-load-plugins": "~1.2.0",
    "gulp-util": "^3.0.7",
    "require-dir": "~0.3.0"
  }
}
