{
  "name": "electron-test",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "build":"electron-packager ./ ElectronTest --out=dist --platform=darwin --arch=x64 --version=0.36.7",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron-packager": "^5.2.1",
    "electron-prebuilt": "^0.36.7"
  }
}
